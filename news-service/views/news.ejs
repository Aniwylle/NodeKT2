<!DOCTYPE html>
<html lang="ru">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>News - <%= category %>
    </title>
    <link rel="stylesheet" href="/style.css">
</head>

<body>
    <div class="container">
        <h2>
            <%= count %> последних новостей в категории <%= category %>
        </h2>
        <dl>
            <% articles.forEach(article=> { %>
                <dt>
                    <%= article.title %>
                </dt>
                <dd>
                    <%= article.description %>
                </dd>
                <% }); %>
        </dl>
    </div>
</body>

</html>